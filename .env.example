# PPPOE 激活系统 Docker 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改

# 应用端口配置（使用标准端口，无需指定端口号）
APP_PORT=80
ADMIN_PORT=80

# 网卡配置（使用空格分隔多个网卡）
# 根据实际硬件设备修改网卡名称
# 常见网卡命名规则：
# - 传统命名：eth0 eth1 eth2 eth3
# - 一致性命名：enp3s0 enp4s0 enp5s0 enp6s0 enp7s0
# - USB网卡：enx开头的MAC地址
# 查看网卡命令：ip link show 或 ifconfig
NETWORK_INTERFACES=eth0 eth1 eth2 eth3

# 时区配置
TZ=Asia/Shanghai

# 数据持久化路径配置
# 数据目录（用于存储数据库）
DATA_PATH=./data

# 日志目录（用于存储日志）
LOGS_PATH=./logs

# 数据库文件路径
DB_PATH=./database.db

# 实例目录路径
INSTANCE_PATH=./instance
